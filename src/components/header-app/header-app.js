import React from 'react';
import { Layout } from 'antd';
import './header-app.css';
const { Header } = Layout;

const HeaderApp = () => {
  return <Header className="headerStyle">HeaderApp</Header>;
};

export default HeaderApp;
